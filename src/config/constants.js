export const IMAGE_CONFIG = {
  MAX_SIZE: 10 * 1024 * 1024, // 10MB
  MIN_SIZE: 1024, // 1KB
  ALLOWED_TYPES: ['image/jpeg', 'image/jpg', 'image/png', 'image/webp'],
  CAMERA_CAPTURE_SIZE: 512, // Tamaño de salida para imágenes capturadas
  CAMERA_PADDING_COLOR: '#FFFFFF', // Color de fondo para el padding si la imagen no es cuadrada
};
